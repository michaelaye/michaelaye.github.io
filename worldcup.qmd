---
pagetitle: "FIFA World Cup 2026 — Group Stage"
toc: false
page-layout: article
format:
  html:
    # Layout-only add-on (custom.scss); colors follow the site theme.
    theme:
      light: [sandstone, custom.scss]
      dark: [superhero, custom.scss]
    grid:
      body-width: 880px
---

<!-- Today's / next matches — generated into _today.md by the refresh script. -->
{{< include _today.md >}}

<p class="lt-note">Kickoff times shown in your local time zone (<span class="lt-tz">your local time</span>).</p>

::: {.wc-hero}
[FIFA World Cup 2026 · Group Stage]{.wc-eyebrow}

# Group phase {.wc-title}

[Forty-eight teams in twelve groups, three host nations.]{.wc-sub}

::: {.wc-rule}
:::
:::

<!-- Groups + standings are static HTML in _groups.md (no Python at build time).
     Refresh from live open data with:  python scripts/update_worldcup_standings.py -->
{{< include _groups.md >}}

::: {.wc-hero .ko-hero}
[FIFA World Cup 2026 · Knockout]{.wc-eyebrow}

## Knockout phase {.wc-title}

[Top two from each group advance, plus the eight best third-placed teams. Slots fill in with real teams as the groups finish.]{.wc-sub}

::: {.wc-rule}
:::
:::

<!-- Knockout bracket is static HTML in _knockout.md (regenerated by the refresh script). -->
{{< include _knockout.md >}}

```{=html}
<script>
// Localize all kickoff <time class="lt"> elements to the viewer's time zone.
(function () {
  function localize() {
    document.querySelectorAll('time.lt[datetime]').forEach(function (el) {
      var d = new Date(el.getAttribute('datetime'));
      if (isNaN(d.getTime())) return;
      el.textContent = d.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
      el.title = d.toLocaleString([], { weekday: 'short', day: 'numeric',
        month: 'short', hour: '2-digit', minute: '2-digit' });
    });
    try {
      var tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      document.querySelectorAll('.lt-tz').forEach(function (el) { el.textContent = tz; });
    } catch (e) {}
  }
  if (document.readyState !== 'loading') localize();
  else document.addEventListener('DOMContentLoaded', localize);
})();
</script>
```
